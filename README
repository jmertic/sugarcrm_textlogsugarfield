This is a SugarField for SugarCRM to handle a TextLog interface, where comments can only append to the normal interface and datetime stamped.

To build, zip up contents of this directory. Install via Module Loader.